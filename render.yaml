services:
  - type: web
    name: aerial-object-classifier
    env: python
    pythonVersion: 3.11
    buildCommand: "pip install -r requirements.txt"
    startCommand: "streamlit run app.py --server.port $PORT --server.address 0.0.0.0"
    plan: starter
    autoDeploy: true
